# Image assets

Drop your app logo, splash icon, and any illustration assets here (e.g.
`logo.png`, `empty_state.svg`). Nothing references this folder yet — it's
wired into `pubspec.yaml` so it's ready when you add them.
